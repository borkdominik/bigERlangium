{
    "name": "biger-langium-vscode",
    "version": "0.1.0",
    "private": "true",
    "license": "MIT",
    "scripts": {
        "prepare": "yarn build:language-server && yarn build:webview && yarn build:extension",
        "build:extension": "yarn --cwd packages/extension",
        "build:language-server": "yarn --cwd packages/language-server",
        "build:webview": "yarn --cwd packages/webview"
    },
    "dependencies": {
        "react": "^19.1.0",
        "react-dom": "^19.1.0"
    }
}
